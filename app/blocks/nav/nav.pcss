.nav {
  display: block;
  width: 100%;
}

.main-nav {
  display: flex;
  justify-content: space-between;
  list-style: none;
  padding-left: 0;
  font-size: 18px;

  &__item {
    text-decoration: none;

    &_active {
      text-decoration: underline;
    }
  }
}
